import os
import cv2
import json


gt_dir = "/data/codes/GodViewICA/vis_data/Json/"+"fd"


def vis_raw_data(video_file, json_dir, saved_pic_dir=None, saved_cropped_pic_dir=None, saved_video=None):
    '''
        params:
            video_file: the raw video file's path
            json_dir: the pre-processing results' directory
            saved_pic_dir: the directory to save the pictures if not None
            saved_cropped_pic_dir: the directory to save the cropped pictures if not None
            saved_video: the path to save the video if not None
        return:
            None
    '''
    pass
